"""For when pip wants to check the date or time.
"""

import datetime


def today_is_later_than(release_year: int, month: int, day: int) -> bool:
    today = datetime.date.today()
    given = datetime.date(release_year, month, day)

    return today > given
